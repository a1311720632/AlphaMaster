# AlphaMaster 交易域 (Trading Context)

AlphaMaster 的核心交易域概念。本文件只记录领域术语，不含实现细节。

## 仓位与信号

**连续仓位 (Continuous Position)**:
策略每 bar 收盘给出的目标仓位，是 `tanh(factor)` 在 (-1, 1) 区间内的实数；不收敛到 {-1, 0, +1}。自动驾驶/实盘必须按此连续值调仓，才能与回测一致。
_避免_: 离散仓位、方向仓位、sign 仓位

**目标仓位 (Target Position)**:
策略当前期望持有的连续仓位。执行层每个 bar 收盘把**实际仓位**向它对齐，对齐的差量就是 turnover。
_避免_: 信号（信号是触发/方向层面的口语词；目标仓位是实数）

**实际仓位 (Actual Position)**:
交易所账上真实持有的仓位（已成交、未平）。目标仓位 ≠ 实际仓位时，下一根 bar 收盘产生调仓。
_避免_: 持仓（口语，含混）

**调仓 (Rebalance)**:
每 bar 收盘把实际仓位推进到目标仓位的动作。与“开仓/平仓”这对离散动作相对——连续模型下只有“调到目标”，没有独立的开/平/反手。

## 一致性

**奇偶性 (Parity)**:
回测与实盘在**策略逻辑层**的一致：同一套 `compute_target_positions`、逐 bar 再平衡、不加单笔止盈止损/保护单覆写。奇偶性**只管逻辑，不管成本**——资金费、真实滑点、部分成交都属奇偶性之外的“执行摩擦”，只在实盘报表披露，不进回测。账户级运营熔断也不算破坏奇偶性。说“完全对齐”时默认指**逻辑奇偶性**，不是盈亏奇偶性。
_避免_: 对齐（口语化、含混）

## 生命周期

**自动驾驶 (Autopilot)**:
规划中的第四步——按第一步挖出的策略，在 paper / testnet / live 三种模式下自动根据信号调仓的实盘执行层。其下单/平仓逻辑必须满足奇偶性。v1 单标的起步，多标的组合为后续扩展（执行后端已支持）。

## 模式与执行

**paper 模式**:
回测核心（连续调仓 + 理想化成本）吃实时收盘 bar，**不下任何订单**。用来回答“策略在实时数据上是否还跟回测一致”（过拟合/漂移检测）；不测执行层。
_避免_: 模拟盘（这是 testnet）

**testnet 模式**:
交易所模拟盘 API，走真实下单/持仓/资金费接口，但用假币。用来验证执行层（下单、部分成交、对账）。
_避免_: paper（paper 不打交易所）

**live 模式**:
真实资金、真实执行。

**执行后端 (Execution Backend)**:
可替换的下单实现。paper / testnet / live 三模式共享同一套 `compute_target_positions` + 逐 bar 调仓核心，只换执行后端（模拟 / 模拟盘 / 真实）。加新交易所 = 加一个后端实现，不动核心。

## 安全

**运营熔断 (Operational Circuit Breaker)**:
为运营安全而**停止或限制交易**的硬开关（最大回撤 kill-switch、断网/数据脏熔断、执行熔断）。它会破坏奇偶性（让实际仓位偏离目标或停止调仓），但属有意为之的运营安全，与策略层覆写（单笔止盈止损）区分开。回撤熔断的**基线（峰值权益）跨进程重启持久化**——重启进程不等于人工重置熔断。
_避免_: 保护单（那是离散模型的、被 ADR-0001 排除的单笔止盈止损）

**执行熔断 (Execution Breaker)**:
下单/平仓在 bar 内重试 3 次仍失败触发的硬停机。语义 = 执行层坏了，与回撤/断连熔断并列；触发后 stay_down 等人工，不自动重拉（放弃对账自愈的修复路径，换人工介入）。**行情断连不是执行熔断**——行情有备用源链，交易无备用交易所。

**告警监控 (Alert Monitoring)**:
只记录/通知、**不动仓位**的监控：本地↔交易所持仓不一致、资金费异常、未成交漂移、孤儿单。不破坏奇偶性。与运营熔断的关键区别：告警监控不改仓位，运营熔断会。

## 运营

**备用源链 (Fallback Chain)**:
OKX → Bybit → 币安 的有序行情源列表。主源连续失败自动切下一家（平价切换：备用源供全窗口 bar，不与主源历史混拼），主源恢复即切回；**全链失败才触发断连熔断**。切换期间的信号漂移属执行摩擦（ADR-0003 口径），披露不进回测。备用源是应急发电机，不是第二台主机。

**心跳 (Heartbeat)**:
引擎向外部 watchdog 服务的定期 ping，负责**整机级**死亡检测（VPS 死机时进程内 watcher 与飞书告警全部失效，只有进程外的眼睛能发现沉默）。分层：进程内 watcher 管进程，外部 watchdog 管机器，交易所逐仓强平管末日。

**预检 (Preflight)**:
testnet/live 启动前的就绪检查：ccxt 可导入、凭据非空、凭据有效（实测 fetch_balance，同时验证 sandbox 指向）、state 三元组匹配。预检不过不启动；testnet 长跑达标项同样以机检清单形式进 live 确认弹窗——上实盘是一个状态，不是仪式。

## 账本

**state 三元组与归属**:
一份运行账本的归属是**一笔钱的一段旅程**。(symbol, timeframe, mode) 任一变更 = 换了一笔钱，旧账本归档（.bak）、新账本空白起算；三元组相同而换策略文件 = 钱还是那笔钱，账本沿用（回撤基线也沿用）。

**热账本 (Hot State)**:
"现在怎么样"——内存态 + 最近 N 根 bar/成交的滚动窗口（cap 1000），前端实时轮询的唯一数据源。小、快、原地覆写。

**冷账本 (Cold Ledger)**:
"发生过什么"——append-only 的 JSONL 流水（bar/成交/运营事件，带类型标签），永不删头。日历、权益曲线、成交记录、事件时间线等审计视图一律读冷账本；长跑后热账本窗口滑出历史不在冷账本中丢失。

**对账 (Reconciliation)**:
把本地记录的持仓与交易所实际持仓核对。每 bar 收盘下单前做一次；以交易所为准计算本期 delta，使连续调仓**天然自愈**漂移（部分成交、手动干预、资金费引起的保证金变动都会被下根 bar 的 delta 自动修正）。孤儿单（交易所有仓、本地无策略）不能靠 delta 自愈，单独走告警 + 可选全平。
_避免_: 对账状态机（那是离散票据模型的做法，连续模型不需要）
